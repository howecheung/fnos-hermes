# 内置官方 Hermes Agent 源码版本（由 CI 在打包时自动覆写为实际 clone 的 tag）
#
# 背景：官方 v0.20.0 (Herald Release, 2026-08-03, tag v2026.8.3) 起停止 PyPI 分发
# （wheel/sdist 被构建守卫拦截），仅支持源码安装。因此本包随 FPK 内置完整源码
# （app/hermes-src，由 CI 从官方仓库 clone 指定 tag），安装时执行
# `uv pip install -e "hermes-src[all]"`（editable 安装不触发 bdist_wheel 守卫）。
#
# PKG_VERSION：本仓库的包版本（写入 manifest）
# HERMES_TAG ：官方源码 tag（CI clone 用）
# HERMES_VERSION：官方源码版本号（安装脚本校验用）
PKG_VERSION=0.21.3-1
HERMES_TAG=v2026.9.14
HERMES_VERSION=0.21.3