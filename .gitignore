# fnOS Hermes Agent — .gitignore
# Last updated for release: v0.21.3-1

# Dependencies
node_modules/

# Runtime secrets / env (generated by the app at runtime)
.env
.env.*
.env.providers
*.local
providers-state.yaml

# Build artifacts
dist/
*.fpk
# 构建缓存（源码 tarball / fnpack 二进制），体积大，绝不入库
tools/.cache/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temp clone of upstream role repo (regenerated via gen_personas.py)
_agency_clone/

# Vendored upstream source (distributed via FPK, not in git)
app/hermes-src/

# FPK packages (distributed via GitHub Releases)
pkg/

# Local scratch/test files
app/ui/_check*.js
app/ui/_dup_test.js
# v0.21.149 隐私加固：个人工作记忆/密码/内部备份/凭据文件绝不入库
WORK-MEMORY.md
init-pack.tgz
_restore021/
askpass*.cmd
webdav-credentials
*.private*
